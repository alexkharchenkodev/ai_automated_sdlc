# Agent Evidence Record

```yaml
role:
status: draft # draft | ready | approved | waived | blocked
title:
owner:
date:
source:
satisfies:
  - evidence item name from tools/ai-sdlc/config/agent_evidence.yaml
links:
  - path-or-url
summary:
risks:
follow_up:
```

Use one record per role artifact. Use `waived` only when the record explains why evidence is intentionally skipped.
